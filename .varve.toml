[[maintainers]]
name = "ShinKouyo"
email = "i@0x0f.dev"

vcs = "none"

[pkgbuild_source]
url = "https://github.com/aminvakil/aur.git"
branch = "master"
directory = "pi"

[collect]
exclude = ["*-debug"]

#[hooks]
#pre_build = ["scripts/prepare.sh"]
#post_build = ["scripts/strip-debug.sh"]
#on_success = ["scripts/notify.sh"]
#on_failure = ["scripts/collect-log.sh"]

[aur]
#name = "pi"
submit = false
